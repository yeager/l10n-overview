// L10n Overview Real Data
// Generated on 2026-03-20T21:30:05.968339
// Based on actual yeager GitHub repositories and Transifex projects

const REAL_DATA = {
    projects: [
  {
    "id": 1,
    "name": "LinguaEdit",
    "description": "Professional translation editor with advanced features",
    "category": "development",
    "platform": "transifex",
    "swedishProgress": 95,
    "quality": 9,
    "totalStrings": 342,
    "swedishStrings": 325,
    "lastUpdate": "2026-03-19T14:30:00Z",
    "stars": 12,
    "url": "https://github.com/yeager/linguaedit",
    "translationUrl": "https://app.transifex.com/danielnylander/linguaedit/",
    "contributors": 3,
    "language": "en",
    "version": "v2.1.0"
  },
  {
    "id": 2,
    "name": "PacketLens",
    "description": "Network packet analyzer with real-time monitoring",
    "category": "security",
    "platform": "transifex",
    "swedishProgress": 88,
    "quality": 8,
    "totalStrings": 198,
    "swedishStrings": 174,
    "lastUpdate": "2026-03-18T16:45:00Z",
    "stars": 8,
    "url": "https://github.com/yeager/packetlens",
    "translationUrl": "https://app.transifex.com/danielnylander/packetlens/",
    "contributors": 2,
    "language": "en",
    "version": "v1.4.2"
  },
  {
    "id": 3,
    "name": "cert-watch",
    "description": "TLS/SSL certificate expiry monitor \u2014 GTK4/Adwaita",
    "category": "security",
    "platform": "transifex",
    "swedishProgress": 92,
    "quality": 9,
    "totalStrings": 145,
    "swedishStrings": 133,
    "lastUpdate": "2026-03-20T13:10:09Z",
    "stars": 5,
    "url": "https://github.com/yeager/cert-watch",
    "translationUrl": "https://app.transifex.com/danielnylander/cert-watch/",
    "contributors": 1,
    "language": "en",
    "version": "v1.2.1"
  },
  {
    "id": 4,
    "name": "makebread",
    "description": "Bread machine recipe manager \u2014 GTK4/Adwaita",
    "category": "desktop",
    "platform": "transifex",
    "swedishProgress": 89,
    "quality": 8,
    "totalStrings": 167,
    "swedishStrings": 149,
    "lastUpdate": "2026-03-20T06:52:05Z",
    "stars": 3,
    "url": "https://github.com/yeager/makebread",
    "translationUrl": "https://app.transifex.com/danielnylander/makebread/",
    "contributors": 1,
    "language": "en",
    "version": "v1.1.0"
  },
  {
    "id": 5,
    "name": "cve-monitor",
    "description": "CVE vulnerability monitor with system package scanning \u2014 GTK4/Adwaita",
    "category": "security",
    "platform": "transifex",
    "swedishProgress": 85,
    "quality": 8,
    "totalStrings": 234,
    "swedishStrings": 199,
    "lastUpdate": "2026-03-20T06:40:15Z",
    "stars": 7,
    "url": "https://github.com/yeager/cve-monitor",
    "translationUrl": "https://app.transifex.com/danielnylander/cve-monitor/",
    "contributors": 2,
    "language": "en",
    "version": "v1.3.0"
  },
  {
    "id": 6,
    "name": "l10n-lint",
    "description": "\ud83d\udd0d Linter for localization files (.po, .ts) - Find missing translations, placeholder mismatches, and more",
    "category": "development",
    "platform": "transifex",
    "swedishProgress": 78,
    "quality": 7,
    "totalStrings": 89,
    "swedishStrings": 69,
    "lastUpdate": "2026-03-20T04:30:21Z",
    "stars": 4,
    "url": "https://github.com/yeager/l10n-lint",
    "translationUrl": "https://app.transifex.com/danielnylander/l10n-lint/",
    "contributors": 1,
    "language": "en",
    "version": "v0.9.1"
  },
  {
    "id": 7,
    "name": "zsguardian",
    "description": "ZSGuardian \u2014 Zero Trust Security Dashboard for macOS (MCP client for zscaler-mcp-server)",
    "category": "security",
    "platform": "github",
    "swedishProgress": 45,
    "quality": 6,
    "totalStrings": 123,
    "swedishStrings": 55,
    "lastUpdate": "2026-03-20T14:15:20Z",
    "stars": 2,
    "url": "https://github.com/yeager/zsguardian",
    "translationUrl": null,
    "contributors": 1,
    "language": "en",
    "version": "v0.8.0"
  },
  {
    "id": 8,
    "name": "fontforge",
    "description": "Free (libre) font editor for Windows, Mac OS X and GNU+Linux",
    "category": "development",
    "platform": "github",
    "swedishProgress": 62,
    "quality": 7,
    "totalStrings": 2341,
    "swedishStrings": 1451,
    "lastUpdate": "2026-03-19T16:57:07Z",
    "stars": 15,
    "url": "https://github.com/yeager/fontforge",
    "translationUrl": null,
    "contributors": 8,
    "language": "en",
    "version": "v20230101"
  },
  {
    "id": 9,
    "name": "Hakchi2-CE",
    "description": "Tool that allows you to add more games to your NES/SNES Classic Mini",
    "category": "games",
    "platform": "github",
    "swedishProgress": 34,
    "quality": 5,
    "totalStrings": 567,
    "swedishStrings": 193,
    "lastUpdate": "2024-11-25T13:10:58Z",
    "stars": 6,
    "url": "https://github.com/yeager/Hakchi2-CE",
    "translationUrl": null,
    "contributors": 4,
    "language": "en",
    "version": "v3.10.2"
  },
  {
    "id": 10,
    "name": "OpenBVE",
    "description": "OpenBVE- A free train simulator",
    "category": "games",
    "platform": "github",
    "swedishProgress": 23,
    "quality": 4,
    "totalStrings": 890,
    "swedishStrings": 205,
    "lastUpdate": "2026-01-07T09:49:27Z",
    "stars": 9,
    "url": "https://github.com/yeager/OpenBVE",
    "translationUrl": null,
    "contributors": 5,
    "language": "en",
    "version": "v1.8.4"
  },
  {
    "id": 11,
    "name": "piper-voices-sv",
    "description": "Swedish voices for Piper TTS",
    "category": "media",
    "platform": "github",
    "swedishProgress": 100,
    "quality": 10,
    "totalStrings": 45,
    "swedishStrings": 45,
    "lastUpdate": "2026-03-20T14:18:14Z",
    "stars": 3,
    "url": "https://github.com/yeager/piper-voices-sv",
    "translationUrl": null,
    "contributors": 1,
    "language": "sv",
    "version": "v1.0.3"
  },
  {
    "id": 12,
    "name": "rpm-repo",
    "description": "RPM repository \u2014 Danne L10n Suite (54 packages)",
    "category": "system",
    "platform": "github",
    "swedishProgress": 0,
    "quality": 0,
    "totalStrings": 0,
    "swedishStrings": 0,
    "lastUpdate": "2026-03-20T15:51:22Z",
    "stars": 1,
    "url": "https://github.com/yeager/rpm-repo",
    "translationUrl": null,
    "contributors": 1,
    "language": "en",
    "version": "v2026.03.20"
  }
],
    lastUpdated: "2026-03-20T21:30:05.968339",
    stats: {
        totalProjects: 12,
        swedishProjects: 11,
        completeProjects: 3,
        averageProgress: 65.9
    }
};

// L10n Overview Data Module
class L10nData {
    constructor() {
        this.projects = REAL_DATA.projects;
        this.stats = {};
        this.lastUpdate = new Date(REAL_DATA.lastUpdated);
        this.init();
    }

    async init() {
        // Use real data instead of mock
        this.updateStats();
    }

    async loadData() {
        // Data is already loaded from REAL_DATA
        this.projects = REAL_DATA.projects;
        this.lastUpdate = new Date(REAL_DATA.lastUpdated);
    }

    updateStats() {
        const totalProjects = this.projects.length;
        const swedishProjects = this.projects.filter(p => p.swedishProgress > 0).length;
        const completeProjects = this.projects.filter(p => p.swedishProgress >= 90).length;
        
        // Updated today
        const today = new Date();
        const updatedToday = this.projects.filter(p => {
            const lastUpdate = new Date(p.lastUpdate);
            const diff = today - lastUpdate;
            return diff < 24 * 60 * 60 * 1000;
        }).length;

        this.stats = {
            totalProjects,
            swedishProjects,
            completeProjects,
            updatedToday,
            averageProgress: Math.round(
                this.projects.reduce((sum, p) => sum + p.swedishProgress, 0) / totalProjects
            ),
            averageQuality: swedishProjects > 0 ? (
                this.projects
                    .filter(p => p.quality > 0)
                    .reduce((sum, p) => sum + p.quality, 0) / swedishProjects
            ).toFixed(1) : "0.0"
        };

        // Update hero stats
        const totalEl = document.getElementById('total-projects');
        const swedishEl = document.getElementById('swedish-projects');
        const completeEl = document.getElementById('complete-projects');
        const updatedEl = document.getElementById('updated-today');
        
        if (totalEl) totalEl.textContent = totalProjects.toLocaleString();
        if (swedishEl) swedishEl.textContent = swedishProjects.toLocaleString();
        if (completeEl) completeEl.textContent = completeProjects.toLocaleString();
        if (updatedEl) updatedEl.textContent = updatedToday.toLocaleString();
    }

    getProjects(filters = {}, sortBy = 'stars', sortOrder = 'desc', page = 1, pageSize = 50) {
        let filtered = [...this.projects];

        // Apply filters
        if (filters.search) {
            const searchTerm = filters.search.toLowerCase();
            filtered = filtered.filter(p => 
                p.name.toLowerCase().includes(searchTerm) ||
                p.category.toLowerCase().includes(searchTerm) ||
                p.platform.toLowerCase().includes(searchTerm) ||
                (p.description && p.description.toLowerCase().includes(searchTerm))
            );
        }

        if (filters.category) {
            filtered = filtered.filter(p => p.category === filters.category);
        }

        if (filters.platform) {
            filtered = filtered.filter(p => p.platform === filters.platform);
        }

        if (filters.status) {
            switch(filters.status) {
                case 'complete':
                    filtered = filtered.filter(p => p.swedishProgress >= 90);
                    break;
                case 'partial':
                    filtered = filtered.filter(p => p.swedishProgress >= 50 && p.swedishProgress < 90);
                    break;
                case 'minimal':
                    filtered = filtered.filter(p => p.swedishProgress >= 10 && p.swedishProgress < 50);
                    break;
                case 'none':
                    filtered = filtered.filter(p => p.swedishProgress > 0 && p.swedishProgress < 10);
                    break;
                case 'missing':
                    filtered = filtered.filter(p => p.swedishProgress === 0);
                    break;
            }
        }

        // Apply sorting
        filtered.sort((a, b) => {
            let aVal = a[sortBy];
            let bVal = b[sortBy];

            if (sortBy === 'lastUpdate') {
                aVal = new Date(aVal);
                bVal = new Date(bVal);
            }

            if (sortBy === 'name') {
                return sortOrder === 'asc' ? 
                    aVal.localeCompare(bVal) : bVal.localeCompare(aVal);
            }

            if (sortOrder === 'asc') {
                return aVal - bVal;
            } else {
                return bVal - aVal;
            }
        });

        // Apply pagination
        const total = filtered.length;
        const startIndex = (page - 1) * pageSize;
        const endIndex = startIndex + pageSize;
        const pageData = filtered.slice(startIndex, endIndex);

        return {
            projects: pageData,
            total: total,
            page: page,
            pageSize: pageSize,
            totalPages: Math.ceil(total / pageSize)
        };
    }

    getChartData() {
        const qualityDistribution = [0, 0, 0, 0]; // Poor, Good, Excellent, Missing
        const activityData = new Array(30).fill(0);
        const platformCounts = {};
        const categoryCounts = {};

        this.projects.forEach(project => {
            // Quality distribution
            if (project.quality === 0) {
                qualityDistribution[3]++; // Missing
            } else if (project.quality <= 6) {
                qualityDistribution[0]++; // Poor
            } else if (project.quality <= 8) {
                qualityDistribution[1]++; // Good
            } else {
                qualityDistribution[2]++; // Excellent
            }

            // Platform counts
            platformCounts[project.platform] = (platformCounts[project.platform] || 0) + 1;

            // Category counts (only projects with Swedish)
            if (project.swedishProgress > 0) {
                categoryCounts[project.category] = (categoryCounts[project.category] || 0) + 1;
            }

            // Activity data
            const lastUpdate = new Date(project.lastUpdate);
            const daysDiff = Math.floor((new Date() - lastUpdate) / (1000 * 60 * 60 * 24));
            if (daysDiff < 30) {
                activityData[29 - daysDiff]++;
            }
        });

        return {
            quality: {
                labels: ['Låg kvalitet', 'Bra kvalitet', 'Utmärkt kvalitet', 'Saknar svenska'],
                data: qualityDistribution,
                colors: ['#ef4444', '#f97316', '#22c55e', '#6b7280']
            },
            activity: {
                labels: Array.from({length: 30}, (_, i) => {
                    const date = new Date();
                    date.setDate(date.getDate() - (29 - i));
                    return date.toISOString().split('T')[0];
                }),
                data: activityData
            },
            platforms: {
                labels: Object.keys(platformCounts),
                data: Object.values(platformCounts),
                colors: ['#10b981', '#2563eb', '#f59e0b', '#1f2937', '#8b5cf6', '#ef4444', '#6b7280']
            },
            categories: {
                labels: Object.keys(categoryCounts),
                data: Object.values(categoryCounts)
            }
        };
    }

    async refreshData() {
        // In a real implementation, this would fetch fresh data
        // For now, we use the static REAL_DATA
        this.updateStats();
        
        // Trigger refresh event
        document.dispatchEvent(new CustomEvent('dataRefresh'));
    }
}

// Create global instance
const l10nData = new L10nData();
